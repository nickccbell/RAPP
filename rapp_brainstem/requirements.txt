flask>=2.0.0
flask-cors>=4.0.0
requests>=2.28.0
python-dotenv>=1.0.0
pyzipper>=0.3.6
# anthropic>=0.25.0  # optional — only needed if you use the Anthropic backend
#                      (the built-in AnthropicBackend uses raw requests, so the
#                       SDK is not required.  Uncomment if you prefer the SDK.)
